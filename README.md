# my-atom-packages-development
###install
apm install --packages-file packages.list
### Instalando o atom via ppa
sudo add-apt-repository ppa:webupd8team/atom
sudo apt-get update
sudo apt-get install atom
